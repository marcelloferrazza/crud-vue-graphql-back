services:
  db:
    image: postgres:14.6-alpine
    container_name: crudmovies
    environment:
      POSTGRES_DB: ${DB_NAME:-crudmovies}
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-a1b2c3d4}
    ports:
      - '${DB_PORT:-5431}:5432'